diff --git a/bootloader_message/include/bootloader_message/bootloader_message.h b/bootloader_message/include/bootloader_message/bootloader_message.h
index 6d25a1f..5eabc13 100644
--- a/bootloader_message/include/bootloader_message/bootloader_message.h
+++ b/bootloader_message/include/bootloader_message/bootloader_message.h
@@ -26,7 +26,12 @@
 // 32K - 64K    System space, used for miscellanious AOSP features. See below.
 // Note that these offsets are admitted by bootloader,recovery and uncrypt, so they
 // are not configurable without changing all of them.
-constexpr size_t BOOTLOADER_MESSAGE_OFFSET_IN_MISC = 0;
+//
+// Hanvon rk3566_eink uses the legacy Rockchip misc layout. Both its stock
+// U-Boot and stock recovery read the bootloader_message at byte offset 0x4000
+// (sector offset 32), rather than the AOSP offset 0. Keep TWRP on the same BCB
+// location so reboot commands are consumed and cleared instead of looping.
+constexpr size_t BOOTLOADER_MESSAGE_OFFSET_IN_MISC = 16 * 1024;
 constexpr size_t VENDOR_SPACE_OFFSET_IN_MISC = 2 * 1024;
 constexpr size_t WIPE_PACKAGE_OFFSET_IN_MISC = 16 * 1024;
 constexpr size_t SYSTEM_SPACE_OFFSET_IN_MISC = 32 * 1024;
